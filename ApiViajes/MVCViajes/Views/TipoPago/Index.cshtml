@model IEnumerable<ApiViajes.Models.TipoPago>
@{
    ViewBag.Title = "TipoPagos";
}

<div class="container">

    <div class="col-sm-12 my-5 text-center">
        <h2 class="font-weight-bold">
            TipoPagos
        </h2>
    </div>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                id
            </th>
            <th>
                Mombre TipoPago
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id_TipoPago)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.NombreTipoPago)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id_TipoPago }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id_TipoPago }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id_TipoPago })
                </td>

            </tr>
        }
        <tr>
            <td>
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            </td>
        </tr>

    </table>

</div>
